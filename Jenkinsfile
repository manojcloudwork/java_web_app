pipeline {
    agent any
	
stages {
        stage('git clone') {
            steps {
                git branch: 'main'url: 'https://github.com/manojcloudwork/java_web_app.git'
            }
        }
		 
	}
